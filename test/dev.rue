
// body{
//     margin: 0;
//     background: var(--l0);
//     color: white;
// }

func getLayer(layer) {
    let base = 20
    let diff = 8
    return `rgb(${20 + layer * diff}, ${20 + layer * diff}, ${20 + layer * diff})`
}

func getColor(r, g, b) {
    return `rgb(${r}, ${g}, ${b})`
}

// def l0: { getLayer(0) }
def l0: rgb(20, 20, 20);
def l1: getLayer(1);
def l2: getLayer(2);
def l3: getLayer(3);
def red: getColor(255, 0, 0);

.module{
    padding: 1.6rem;
    margin: 1.6rem;
    background: var(--l1);
    text-align: center;
    border-radius: 0.4rem;
    cursor: pointer;

    :hover{
        background: var(--l2);
    }

    .title{
        font-size: 1.5rem;
        font-weight: 600;
    }

    .description{
        opacity: 0.75;
    }
}